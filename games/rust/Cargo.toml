[package]
name = "snake-rs"
version = "1.0.0"
edition = "2021"
description = "Snake game logic compiled to freestanding WebAssembly"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
strip = true
